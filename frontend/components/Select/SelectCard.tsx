import { Card } from "../ui/card";
import Select from "./Select";

const SelectCard = () => {
  return (
    <Card className="w-[30%] m-auto mt-[10vh]">
      <Select />
    </Card>
  );
};

export default SelectCard;
